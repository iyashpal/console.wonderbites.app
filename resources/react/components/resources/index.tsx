import ResourceList from './ResourceList'
import ResourceForm from './ResourceForm'
import ResourceDetail from './ResourceDetail'
export { Table, TableHead, TableBody, TableRow, TableHeadCell, TableDataCell, TableHeadCheckboxCell, TableDataCheckboxCell, ActionBar, SearchInput, SortByFilter, RangeFilter } from './ResourceList'
const Resources = {
    List: ResourceList,
    Form: ResourceForm,
    Detail: ResourceDetail,
}

export default Resources
